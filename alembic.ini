[alembic]
script_location = alembic
prepend_sys_path = src
path_separator = os

# No sqlalchemy.url here on purpose. `alembic/env.py` reads the same
# `store.engine.database_url()` the application does, so a migration cannot be applied to a
# different database than the one the code talks to — which is exactly the mistake a duplicated DSN
# in two files eventually produces.

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
